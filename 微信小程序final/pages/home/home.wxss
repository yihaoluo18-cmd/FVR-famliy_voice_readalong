
/* 容器 */
.container {
  min-height: 100vh;
  background: linear-gradient(to bottom, #fdf2f8, #fff);
  padding: 32rpx;
  padding-bottom: 30rpx;
}

/* 顶部：伴宠 3D + 信息 */
.home-hero {
  display: flex;
  gap: 22rpx;
  align-items: flex-start;
  margin: 12rpx 0 44rpx;
}

.home-pet-card {
  width: 330rpx;
  height: 360rpx;
  border-radius: 44rpx;
  background: rgba(255, 255, 255, 0.82);
  border: 3rpx solid rgba(255, 255, 255, 0.9);
  box-shadow: 0 10rpx 30rpx rgba(232, 121, 169, 0.12);
  position: relative;
  overflow: visible;
}

.home-pet-card-switching {
  animation: homeCardSwitchPulse 0.9s ease-in-out infinite;
}

.home-pet-name {
  font-size: 52rpx;
  font-weight: 800;
  color: #ff8a4d;
  text-shadow: 0 3rpx 10rpx rgba(255, 180, 140, 0.28);
  position: relative;
  z-index: 1;
}

.home-pet-name-row {
  display: inline-flex;
  align-items: center;
  margin: 6rpx 0 24rpx 8rpx;
  padding: 8rpx 24rpx;
  border-radius: 22rpx;
  background: linear-gradient(145deg, rgba(255, 248, 252, 0.94), rgba(255, 238, 246, 0.9));
  border: 2rpx solid rgba(250, 214, 231, 0.95);
  box-shadow: 0 8rpx 20rpx rgba(232, 121, 169, 0.12);
}
.home-tune-fab {
  position: absolute;
  right: 12rpx;
  top: 12rpx;
  z-index: 4;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  background: linear-gradient(160deg, rgba(255, 255, 255, 0.9), rgba(245, 236, 255, 0.72));
  border: 2rpx solid rgba(255, 255, 255, 0.94);
  color: #e879a9;
  font-size: 20rpx;
  font-weight: 800;
  box-shadow:
    0 8rpx 18rpx rgba(232, 121, 169, 0.18),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.75),
    inset 0 -2rpx 4rpx rgba(232, 121, 169, 0.08);
}

.home-tune-fab:active {
  transform: translateY(1rpx) scale(0.97);
}

.home-switch-fab {
  position: absolute;
  right: 94rpx;
  top: 12rpx;
  z-index: 4;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  background: linear-gradient(160deg, rgba(255, 255, 255, 0.9), rgba(245, 236, 255, 0.72));
  border: 2rpx solid rgba(255, 255, 255, 0.94);
  color: #e879a9;
  font-size: 20rpx;
  font-weight: 800;
  box-shadow:
    0 8rpx 18rpx rgba(232, 121, 169, 0.18),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.75),
    inset 0 -2rpx 4rpx rgba(232, 121, 169, 0.08);
}

.home-switch-fab:active {
  transform: translateY(1rpx) scale(0.97);
}

.home-pet-stage {
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  border-radius: 44rpx;
  overflow: hidden;
}

/* 模型点击引导：手势图 + 文案，浮动提示 */
.home-tap-hint {
  position: absolute;
  left: 50%;
  bottom: 14rpx;
  transform: translateX(-50%);
  z-index: 7;
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 12rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.9);
  border: 2rpx solid rgba(252, 231, 243, 0.95);
  box-shadow: 0 8rpx 18rpx rgba(232, 121, 169, 0.18);
  animation: homeTapHintFloat 1.35s ease-in-out infinite;
  pointer-events: none;
}

.home-tap-hint-icon {
  width: 24rpx;
  height: 24rpx;
  filter: brightness(0.6);
}

.home-tap-hint-text {
  font-size: 20rpx;
  font-weight: 800;
  color: #b87a94;
  line-height: 1;
  white-space: nowrap;
}

@keyframes homeTapHintFloat {
  0%, 100% { transform: translateX(-50%) translateY(0); opacity: 0.92; }
  50% { transform: translateX(-50%) translateY(-4rpx); opacity: 1; }
}

.home-switching-overlay {
  position: absolute;
  inset: 0;
  z-index: 6;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  background: linear-gradient(165deg, rgba(255,255,255,0.28), rgba(255,248,250,0.18));
  backdrop-filter: blur(1px);
  -webkit-backdrop-filter: blur(1px);
  pointer-events: none;
}

.home-switching-ring {
  width: 42rpx;
  height: 42rpx;
  border-radius: 50%;
  border: 4rpx solid rgba(251,207,232,0.55);
  border-top-color: rgba(244,194,212,0.96);
  animation: homeSwitchSpin 0.75s linear infinite;
}

.home-switching-text {
  font-size: 20rpx;
  font-weight: 800;
  color: rgba(176,112,132,0.9);
  text-shadow: 0 2rpx 8rpx rgba(232,121,169,0.1);
  animation: homeSwitchTextPulse 1s ease-in-out infinite;
}

.home-pet-shadow {
  position: absolute;
  left: 50%;
  bottom: -12rpx;
  transform: translateX(-50%);
  width: 160rpx;
  height: 30rpx;
  background: rgba(251, 207, 232, 0.28);
  border-radius: 50%;
  filter: blur(10rpx);
}

.home-pet-meta {
  flex: none;
  width: 330rpx;   /* 与左侧展示框同宽 */
  height: 360rpx;  /* 与左侧展示框同高 */
  min-width: 0;
  padding-top: 0;
  position: relative;
}

.status-title-wrap {
  position: absolute;
  left: 0;
  right: 0;
  top: -98rpx;
  z-index: 3;
  display: flex;
  justify-content: center;
  pointer-events: none;
}

.status-title-badge {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  padding: 10rpx 24rpx;
  border-radius: 999rpx;
  background: linear-gradient(160deg, rgba(255, 255, 255, 0.94), rgba(252, 231, 243, 0.92));
  border: 2rpx solid rgba(255, 255, 255, 0.95);
  box-shadow:
    0 8rpx 20rpx rgba(232, 121, 169, 0.14),
    inset 0 2rpx 6rpx rgba(255, 255, 255, 0.7);
}

/* ===== 首页切换弹窗（居中） ===== */
.home-switch-mask{
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  background: rgba(17, 24, 39, 0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 26rpx;
  box-sizing: border-box;
}

.home-switch-modal{
  width: 620rpx;
  max-width: calc(100% - 36rpx);
  border-radius: 28rpx;
  background: rgba(255,255,255,0.92);
  border: 2rpx solid rgba(252, 231, 243, 0.9);
  box-shadow: 0 22rpx 70rpx rgba(232, 121, 169, 0.22);
  overflow: hidden;
}

.home-switch-header{
  display:flex;
  align-items:center;
  justify-content: space-between;
  padding: 20rpx 20rpx 12rpx;
}

.home-switch-title{
  font-size: 30rpx;
  font-weight: 900;
  color: #9d6b7f;
}

.home-switch-close{
  width: 54rpx;
  height: 54rpx;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius: 999rpx;
  background: rgba(245, 236, 255, 0.8);
  border: 2rpx solid rgba(252, 231, 243, 0.9);
  color:#e879a9;
  font-weight: 900;
}

.home-switch-menu{
  padding: 10rpx 20rpx 22rpx;
  display:flex;
  flex-direction: column;
  gap: 14rpx;
}

.home-switch-item{
  padding: 18rpx 18rpx;
  border-radius: 22rpx;
  background: linear-gradient(165deg, rgba(255,255,255,0.92), rgba(255,248,250,0.62));
  border: 2rpx solid rgba(252, 231, 243, 0.9);
}

.home-switch-item-title{
  display:block;
  font-size: 28rpx;
  font-weight: 900;
  color:#9d4e6e;
}

.home-switch-item-sub{
  display:block;
  margin-top: 6rpx;
  font-size: 22rpx;
  color: rgba(107,114,128,0.95);
  font-weight: 700;
}

.home-switch-body{
  padding: 8rpx 20rpx 22rpx;
}

.home-switch-back{
  display:inline-block;
  padding: 10rpx 10rpx;
  font-size: 22rpx;
  font-weight: 900;
  color:#f0a8c0;
}

.home-switch-grid{
  display:flex;
  flex-wrap: wrap;
  gap: 16rpx;
  padding-top: 10rpx;
}

.home-switch-mascot{
  width: 180rpx;
  border-radius: 22rpx;
  background: rgba(255,255,255,0.9);
  border: 2rpx solid rgba(252,231,243,0.95);
  padding: 12rpx;
  position: relative;
  box-sizing: border-box;
}

.home-switch-mascot-img{
  width: 100%;
  height: 120rpx;
  border-radius: 18rpx;
}

.home-switch-mascot-name{
  margin-top: 8rpx;
  font-size: 22rpx;
  font-weight: 600;
  color: #ff8a4d;
  text-align:center;
}

.mascot-name-cute-dog { color: #f97316; }
.mascot-name-cute-fox { color: #fb7185; }
.mascot-name-cute-dino { color: #22c55e; }
.mascot-name-cute-cat { color: #a855f7; }
.mascot-name-cute-bunny { color: #ec4899; }
.mascot-name-cute-squirrel { color: #d97706; }
.mascot-name-cute-chick { color: #eab308; }
.mascot-name-cute-panda { color: #6b7280; }
.mascot-name-cute-koala { color: #65a30d; }
.mascot-name-cute-penguin { color: #0ea5e9; }

.home-switch-lock{
  position:absolute;
  right: 10rpx;
  top: 10rpx;
  font-size: 22rpx;
}

.home-switch-locked{
  opacity: 0.72;
}

.home-switch-img-dim{
  filter: grayscale(0.6);
  opacity: 0.72;
}

.home-form-grid{
  display:flex;
  gap: 16rpx;
  padding-top: 10rpx;
}

.home-form-card{
  flex: 1;
  border-radius: 22rpx;
  background: rgba(255,255,255,0.94);
  border: 2rpx solid rgba(255,245,248,0.98);
  padding: 12rpx;
  position: relative;
  box-sizing: border-box;
  box-shadow: 0 4rpx 14rpx rgba(244, 194, 212, 0.08);
}

.home-form-poster{
  position: relative;
}

.home-form-poster-img{
  width: 100%;
  height: 150rpx;
  border-radius: 18rpx;
}

.home-form-badge{
  position:absolute;
  left: 10rpx;
  bottom: 10rpx;
  padding: 6rpx 10rpx;
  border-radius: 999rpx;
  background: rgba(252,231,243,0.98);
  border: 1rpx solid rgba(251,207,232,0.65);
  color:#b87a94;
  font-size: 20rpx;
  font-weight: 900;
}

.home-form-title{
  margin-top: 10rpx;
  font-size: 22rpx;
  font-weight: 900;
  color:#c9a8b5;
  text-align:center;
}

/* 状态区：统一放在同一块主题底板上 */
.status-panel {
  width: 100%;
  height: 100%;
  border-radius: 44rpx; /* 与左侧展示框圆角一致 */
  padding: 20rpx 18rpx 16rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.62);
  box-shadow: 0 12rpx 30rpx rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(6rpx);
  -webkit-backdrop-filter: blur(6rpx);
  box-sizing: border-box;
}

/* 整体状态布局（2×2） */
.status-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  grid-template-rows: repeat(2, 1fr);
  gap: 16rpx 18rpx;
  width: 100%;
  height: 100%;
  justify-items: center;
  align-items: center;
}

.status-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 圆形进度条外框 */
.status-circle {
  width: 116rpx;
  height: 116rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 👇 外圈灰色底（保留，用于承载进度条） */
.status-circle-bg {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: #f7ebf2;
}

/* 👇 彩色进度条（保持不变） */
.status-circle-progress {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: conic-gradient(var(--color) var(--progress), transparent 0deg);
}

/* 中间渐变背景：基础样式 */
.status-circle::after {
  content: "";
  position: absolute;
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  z-index: 1;
  background: rgba(255, 255, 255, 0.58);
  border: 2rpx solid rgba(255, 255, 255, 0.72);
  box-shadow:
    inset 0 8rpx 18rpx rgba(255, 255, 255, 0.48),
    inset 0 -6rpx 14rpx rgba(214, 160, 188, 0.22),
    0 4rpx 10rpx rgba(232, 121, 169, 0.08);
  backdrop-filter: blur(10rpx);
  -webkit-backdrop-filter: blur(10rpx);
}


.status-hunger .status-circle::after {
  background: rgba(255, 255, 255, 0.58);
}


.status-clean .status-circle::after {
  background: rgba(255, 255, 255, 0.58);
}


.status-mood .status-circle::after {
  background: rgba(255, 255, 255, 0.58);
}

.status-sleepy .status-circle::after {
  background: rgba(255, 255, 255, 0.58);
}


.status-circle-content {
  position: relative;
  z-index: 3;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.mini-emoji {
  font-size: 32rpx;
  margin-bottom: 2rpx;
  line-height: 1;
}
.mini-icon {
  width: 36rpx;
  height: 36rpx;
  margin-bottom: 0;
  opacity: 0.98;
  /* 原图为浅灰，磨砂底上会发虚：统一加深到可读 */
  filter: brightness(0.35) contrast(1.25);
}

.status-value {
  margin-top: 10rpx;
  font-size: 22rpx;
  font-weight: 900;
  color: rgba(40, 40, 55, 0.62);
  letter-spacing: 0.4rpx;
}

.mini-ball {
  width: 70rpx;
  height: 70rpx;
  border-radius: 999rpx;
  position: relative;
  overflow: hidden;
  border: 2rpx solid rgba(255, 255, 255, 0.95);
  box-shadow:
    0 10rpx 20rpx rgba(232, 121, 169, 0.13),
    inset 0 3rpx 8rpx rgba(255, 255, 255, 0.84),
    inset 0 -4rpx 10rpx rgba(232, 121, 169, 0.08);
  background: linear-gradient(165deg, rgba(255, 255, 255, 0.82), rgba(245, 236, 255, 0.5));
  backdrop-filter: blur(8rpx);
  -webkit-backdrop-filter: blur(8rpx);
}

.mini-ball::before {
  content: "";
  position: absolute;
  left: 10rpx;
  right: 12rpx;
  top: 8rpx;
  height: 18rpx;
  border-radius: 999rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(255, 255, 255, 0.2));
  filter: blur(0.5rpx);
  z-index: 2;
  pointer-events: none;
}

.mini-ball::after {
  content: "";
  position: absolute;
  width: 12rpx;
  height: 12rpx;
  right: 16rpx;
  top: 18rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.8);
  z-index: 2;
  pointer-events: none;
}

.mini-liquid {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 999rpx;
  opacity: 0.95;
  transition: height 0.35s ease;
}

.mini-emoji {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  filter: drop-shadow(0 6rpx 10rpx rgba(0, 0, 0, 0.12));
  z-index: 3;
}

.mini-ball-food .mini-liquid { background: linear-gradient(180deg, #fde68a, #f59e0b); }
.mini-ball-clean .mini-liquid { background: linear-gradient(180deg, #fef9c3, #facc15); }
.mini-ball-mood .mini-liquid { background: linear-gradient(180deg, #fde68a, #fbbf24); }
.mini-ball-sleepy .mini-liquid { background: linear-gradient(180deg, #fef3c7, #f59e0b); }

.home-voice-row {
  display: flex;
  justify-content: flex-start;
  opacity: 0.98;
}

.home-tuning-panel {
  margin: 10rpx 0 24rpx;
  border-radius: 28rpx;
  background:
    radial-gradient(160rpx 90rpx at 20% 8%, rgba(255, 255, 255, 0.35), rgba(255, 255, 255, 0)),
    linear-gradient(150deg, rgba(255, 255, 255, 0.9), rgba(245, 236, 255, 0.72));
  border: 3rpx solid rgba(255, 255, 255, 0.92);
  box-shadow:
    0 16rpx 32rpx rgba(232, 121, 169, 0.15),
    inset 0 2rpx 8rpx rgba(255, 255, 255, 0.82),
    inset 0 -6rpx 14rpx rgba(232, 121, 169, 0.08);
  padding: 18rpx 18rpx 16rpx;
  backdrop-filter: blur(12rpx);
  -webkit-backdrop-filter: blur(12rpx);
}

.home-tuning-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 8rpx;
}

.home-tuning-title {
  color: #b87a94;
  font-size: 22rpx;
  font-weight: 800;
}

.home-tuning-close {
  color: #6b7280;
  font-size: 28rpx;
  font-weight: 700;
  padding: 4rpx 10rpx;
}

.home-tune-row {
  margin: 8rpx 0 6rpx;
}

.home-tune-label {
  display: block;
  color: #4b5563;
  font-size: 21rpx;
  font-weight: 700;
  margin-bottom: 4rpx;
}

.home-tune-input-row {
  margin-top: 10rpx;
  display: flex;
  justify-content: flex-start;
  align-items: center;
}

.home-tune-input {
  /* 小程序 input 默认宽度不稳定，显式给宽，避免遮挡面板 */
  width: 240rpx;
  height: 56rpx;
  padding: 0 16rpx;
  border-radius: 16rpx;
  border: 3rpx solid rgba(255, 255, 255, 0.9);
  background: rgba(255, 255, 255, 0.75);
  font-size: 26rpx;
  font-weight: 800;
  box-shadow:
    0 8rpx 18rpx rgba(232, 121, 169, 0.08),
    inset 0 2rpx 6rpx rgba(255, 255, 255, 0.75);
}

.home-tuning-actions {
  margin-top: 8rpx;
  display: flex;
  justify-content: flex-end;
  gap: 10rpx;
}

.home-tuning-btn {
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 800;
  border: 2rpx solid rgba(255, 255, 255, 0.86);
  box-shadow:
    0 8rpx 16rpx rgba(232, 121, 169, 0.14),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.7);
}

.home-tuning-btn:active {
  transform: translateY(1rpx) scale(0.98);
}

.btn-light {
  color: #6b7280;
  background: linear-gradient(160deg, rgba(255, 255, 255, 0.88), rgba(243, 244, 246, 0.82));
}

.btn-primary {
  color: #ffffff;
  background: linear-gradient(135deg, #f0a3c2, #e879a9);
}

/* 中部：两张进度卡 */
.home-metrics-row {
  display: flex;
  gap: 18rpx;
  margin: 0 0 42rpx;
}

.metric-card {
  flex: 1;
  background: linear-gradient(145deg, #fffdf2 0%, #fff7d6 100%);
  border-radius: 36rpx;
  border: 3rpx solid rgba(255, 245, 194, 0.95);
  box-shadow: 0 10rpx 24rpx rgba(226, 186, 76, 0.12);
  padding: 22rpx 18rpx 18rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.metric-status-circle {
  width: 150rpx;
  height: 150rpx;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.metric-status-circle-bg {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: #f7ebf2;
}

.metric-status-circle-progress {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: conic-gradient(#d977a0 var(--progress), transparent 0deg);
}

.metric-status-circle-content {
  position: relative;
  z-index: 3;
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.58);
  border: 2rpx solid rgba(255, 255, 255, 0.72);
  box-shadow:
    inset 0 8rpx 18rpx rgba(255, 255, 255, 0.48),
    inset 0 -6rpx 14rpx rgba(214, 160, 188, 0.22),
    0 4rpx 10rpx rgba(232, 121, 169, 0.08);
  backdrop-filter: blur(10rpx);
  -webkit-backdrop-filter: blur(10rpx);
  display: flex;
  align-items: center;
  justify-content: center;
}

.metric-status-icon {
  width: 58rpx;
  height: 58rpx;
  filter: brightness(0.9) saturate(1.05);
}

.metric-status-value {
  margin-top: 2rpx;
  font-size: 34rpx;
  font-weight: 900;
  color: #9d6b7f;
}

.metric-label {
  color: #a8989e;
  font-size: 22rpx;
  font-weight: 700;
  text-align: center;
}

/* 四快捷入口 */
.quick-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 22rpx;
  margin-bottom: 48rpx;
}

.quick-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.quick-item-locked {
  opacity: 0.48;
  pointer-events: auto;
}

.quick-icon-ball {
  width: 104rpx;
  height: 104rpx;
  border-radius: 24rpx;
  border: 1.5rpx solid rgba(221, 226, 233, 0.95);
  box-shadow:
    0 6rpx 14rpx rgba(15, 23, 42, 0.07),
    0 1rpx 2rpx rgba(15, 23, 42, 0.05),
    inset 0 1rpx 2rpx rgba(255, 255, 255, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(2rpx);
  -webkit-backdrop-filter: blur(2rpx);
  transition: all 0.2s ease;
  background: linear-gradient(160deg, #ffffff, #f6f7f9);
}


.quick-icon-ball::before {
  content: none;
}


.quick-icon-ball::after {
  content: none;
}
/* 🖱️ 按下时的3D按压效果 */
.quick-icon-ball:active {
  transform: translateY(1rpx) scale(0.985);
  box-shadow:
    0 3rpx 8rpx rgba(15, 23, 42, 0.09),
    inset 0 1rpx 3rpx rgba(15, 23, 42, 0.06),
    inset 0 -1rpx 3rpx rgba(255, 255, 255, 0.7);
}


/* 演说家（麦克风）：紫蓝渐变 → id = speaker */
.quick-icon-ball.ball-speaker {
  background: linear-gradient(160deg, #ffffff, #f6f7f9);
}

/* 涂色（蜡笔）：黄绿渐变 → id = color */
.quick-icon-ball.ball-color {
  background: linear-gradient(160deg, #ffffff, #f6f7f9);
}

/* 小柴伙伴（小狗）：暖棕渐变 → id = companion */
.quick-icon-ball.ball-companion {
  background: linear-gradient(160deg, #ffffff, #f6f7f9);
}

/* 宠物蛋（蛋）：橙黄渐变 → id = petEggGarden */
.quick-icon-ball.ball-petEggGarden {
  background: linear-gradient(160deg, #ffffff, #f6f7f9);
}

.quick-icon {
  font-size: 44rpx;
  position: relative;
  z-index: 2;
  filter: drop-shadow(0 2rpx 4rpx rgba(0, 0, 0, 0.15));
}
.quick-icon-img {
  width: 46rpx;
  height: 46rpx;
  position: relative;
  z-index: 2;
  filter: drop-shadow(0 1rpx 2rpx rgba(0, 0, 0, 0.1));
}

.quick-title {
  color: #a8989e;
  font-size: 21rpx;
  font-weight: 700;
  text-align: center;
  letter-spacing: 0.4rpx;
}

/* 底部“绘本阅读”恢复原设计：上下卡片 */
.reading-stack {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.reading-menu-item {
  position: relative;
  display: flex;
  align-items: center;
  gap: 26rpx;
  border-radius: 42rpx;  
  border: 3rpx solid rgba(255, 255, 255, 0.85);
    box-shadow: 
    0 12rpx 32rpx rgba(0, 0, 0, 0.08),
    inset 0 2rpx 8rpx rgba(255, 255, 255, 0.9),
    inset 0 -4rpx 12rpx rgba(0, 0, 0, 0.06);
  padding: 24rpx 22rpx;
  overflow: hidden;
  
  background-image: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.7) 0%, 
    rgba(255, 255, 255, 0.4) 100%
  );
  backdrop-filter: blur(10rpx);
  -webkit-backdrop-filter: blur(10rpx);
}


.reading-books {
  background: linear-gradient(135deg, 
    rgba(255, 245, 247, 0.95) 0%, 
    rgba(254, 243, 199, 0.55) 100%
  );
}


.reading-voice {
  background: linear-gradient(135deg, 
    rgba(252, 231, 243, 0.9) 0%, 
    rgba(251, 207, 232, 0.65) 100%
  );
}

.reading-menu-icon-box {
  width: 104rpx;
  height: 120rpx;
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.85);
  box-shadow: 
    inset 0 2rpx 6rpx rgba(255, 255, 255, 0.9),
    inset 0 -2rpx 4rpx rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.reading-menu-icon {
  font-size: 52rpx;
}
.reading-menu-icon-img {
  width: 54rpx;
  height: 54rpx;
}

.reading-menu-text {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}

.reading-menu-title {
  color: #9d6b7f;
  font-size: 42rpx;
  font-weight: 900;
  letter-spacing: 1rpx;

  text-shadow: 0 2rpx 4rpx rgba(232, 121, 169, 0.08);
}

.reading-menu-desc {
  color: #a8989e;
  font-size: 24rpx;
  font-weight: 700;
}

.reading-menu-sticker {
  position: absolute;
  right: 12rpx;
  top: 50%;
  transform: translateY(-50%);
  pointer-events: none;
  opacity: 0.95;
 
  filter: drop-shadow(0 6rpx 12rpx rgba(0, 0, 0, 0.1));
}

.reading-sticker-book {
  width: 82rpx;
  height: 82rpx;
  right: 24rpx;
}

.reading-sticker-mic {
  width: 96rpx;
  height: 96rpx;
  right: 26rpx;
  opacity: 0.92;
}

/* 欢迎栏 */
.welcome-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 32rpx;
  animation: slideDown 0.5s ease-out;
}

@keyframes slideDown {
  from { opacity: 0; transform: translateY(-20rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.user-info {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  border: 6rpx solid white;
  box-shadow: 0 4rpx 12rpx rgba(240, 163, 194, 0.2);
}

.user-details {
  display: flex;
  flex-direction: column;
}

.baby-name {
  color: #b87a94;
  font-size: 28rpx;
  font-weight: 900;
}

.stars-info {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 4rpx;
}

.star-icon {
  font-size: 24rpx;
}
.star-icon-img {
  width: 28rpx;
  height: 28rpx;
}

.stars-text {
  color: #ca8a04;
  font-size: 22rpx;
  font-weight: 700;
}

.title-section {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 0;
}

.sparkle {
  font-size: 36rpx;
  animation: sparkle 2s ease-in-out infinite;
}
.sparkle-icon {
  width: 30rpx;
  height: 30rpx;
  animation: sparkle 2s ease-in-out infinite;
}

@keyframes sparkle {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.7; transform: scale(1.1); }
}

@keyframes homeCardSwitchPulse {
  0%, 100% { transform: translateY(0) scale(1); }
  50% { transform: translateY(-2rpx) scale(1.008); }
}

@keyframes homeSwitchSpin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes homeSwitchTextPulse {
  0%, 100% { opacity: 0.8; transform: translateY(0); }
  50% { opacity: 1; transform: translateY(-1rpx); }
}
.app-title {
  font-family: var(--app-font-family) !important;
  color: #cf769e;
  font-size: 30rpx;
  font-weight: 900;
  letter-spacing: 1.5rpx;
}

/* 吉祥物区域 */

.photo-3d-container {
  position: relative;
  cursor: pointer;
  width: 300rpx;
  height: 330rpx; 
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
  -webkit-tap-highlight-color: transparent;
}
/* 固定宽高比容器，保证所有角度图片大小一致 */
.img-wrapper {
  width: 100%;
  height: 100%;
  position: relative;
  background: transparent; 
  display: flex;
  align-items: center;
  justify-content: center;
}

.home-three-interact-layer {
  position: relative;
  width: 100%;
  height: 100%;
  z-index: 2;
}

.home-three-canvas {
  width: 100%;
  height: 100%;
  display: block;
}
.photo-3d-img {
  width: 100%;
  height: 100%;
  object-fit: contain; /* 完整显示图片，不裁剪 */
  position: static; 
  transition: transform 0.3s ease;
}
.photo-3d-img.small-first {
  
  transform: scale(0.8);
  transform-origin: center;
}
.photo-3d-img.small-second {
  
  transform: scale(0.9);
  transform-origin: center;
}

.photo-3d-img.small-fox {
  transform: scale(0.8);
  transform-origin: center;
}
/* 阴影 */
.mascot-shadow {
  position: absolute;
  bottom: -12rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 160rpx;
  height: 32rpx;
  background: rgba(251, 207, 232, 0.3);
  border-radius: 50%;
  filter: blur(8rpx);
}

/* 提示文字样式 */
.mascot-hint {
  margin-top: 20rpx;
  text-align: center;
  animation: fadeIn 0.5s ease-out 0.5s both;
}
.hint-text {
  color: #f0a8c0;
  font-size: 24rpx;
  font-weight: 700;
}
.mascot-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 24rpx;
  position: relative;
}

.mascot-container {
  position: relative;
  cursor: pointer;
}

.floatingClass {
  animation: float 4s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-16rpx); }
}

.mascot-image {
  width: 224rpx;
  height: 224rpx;
  border-radius: 50%;
  border: 8rpx solid white;
  box-shadow: 0 8rpx 24rpx rgba(240, 163, 194, 0.3);
  background: #fdf2f8;
}

.mascot-shadow {
  position: absolute;
  bottom: -12rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 160rpx;
  height: 32rpx;
  background: rgba(251, 207, 232, 0.3);
  border-radius: 50%;
  filter: blur(8rpx);
}

.speech-bubble {
  position: absolute;
  top: -25rpx;
  right: -82rpx;
  background: white;
  padding: 8rpx 16rpx;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  border: 4rpx solid #fce7f3;
  font-size: 22rpx;
  font-weight: 900;
  color: #e879a9;
  white-space: nowrap;
}

.bubbleClass {
  animation: bubble 3s ease-in-out infinite;
}

@keyframes bubble {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.7; transform: scale(1.03); }
}

.mascot-hint {
  margin-top: 16rpx;
  text-align: center;
  animation: fadeIn 0.5s ease-out 0.5s both;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.hint-text {
  color: #f0a8c0;
  font-size: 24rpx;
  font-weight: 700;
}

/* 头像选择器 */
.avatar-picker {
  width: 100%;
  margin-top: 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 48rpx;
  border: 4rpx solid #fef3c7;
  box-shadow: 0 8rpx 32rpx rgba(240, 163, 194, 0.2);
  padding: 24rpx;
  transition: all 0.3s ease;
}

.avatar-picker.hide {
  opacity: 0;
  transform: scale(0.95) translateY(-20rpx);
  pointer-events: none;
}

.avatar-picker.show {
  opacity: 1;
  transform: scale(1) translateY(0);
  animation: slideIn 0.3s ease-out;
}

@keyframes slideIn {
  from { opacity: 0; transform: scale(0.95) translateY(-20rpx); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

.picker-title {
  text-align: center;
  margin-bottom: 16rpx;
}

.picker-title text {
  color: #e879a9;
  font-size: 24rpx;
  font-weight: 900;
}

.avatar-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
}

.avatar-option {
  position: relative;
  border-radius: 32rpx;
  overflow: hidden;
  border: 6rpx solid white;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  transition: all 0.2s;
  height: 200rpx;
}

.avatar-option.selected {
  border-color: #f0a3c2;
  box-shadow: 0 6rpx 20rpx rgba(240, 163, 194, 0.4);
}

.option-image {
  width: 100%;
  aspect-ratio: 1;
  background: #fdf2f8;
  height:250rpx;
}

.option-name {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.5), transparent);
  padding: 8rpx 0;
  text-align: center;
  color: white;
  font-size: 20rpx;
  font-weight: 900;
}

.selected-mark {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 40rpx;
  height: 40rpx;
  background: #f0a3c2;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid white;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
  color: white;
  font-size: 24rpx;
  font-weight: 900;
  animation: pop 0.3s ease-out;
}

@keyframes pop {
  0% { transform: scale(0); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}

/* 等级卡片 */
.level-card {
  width: 90%;
  background: linear-gradient(135deg, #fce7f3 0%, #fce7f3 100%);
  border: 4rpx solid white;
  border-radius: 48rpx;
  box-shadow: 0 4rpx 16rpx rgba(240, 163, 194, 0.15);
  padding: 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 40rpx;
  animation: slideIn 0.5s ease-out 0.1s both;
}

.level-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.level-icon-box {
  width: 112rpx;
  height: 112rpx;
  background: linear-gradient(135deg, #fbcfe8 0%, #fbcfe8 100%);
  border-radius: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid white;
  box-shadow: 0 2rpx 8rpx rgba(240, 163, 194, 0.1);
}

.level-icon {
  font-size: 60rpx;
}

.level-info {
  display: flex;
  flex-direction: column;
}

.level-title {
  color: #b87a94;
  font-size: 30rpx;
  font-weight: 900;
}

.level-stars {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 4rpx;
}

.trophy-icon {
  font-size: 24rpx;
}

.level-stars-text {
  color: #a16207;
  font-size: 22rpx;
  font-weight: 700;
}

.level-button {
  background: white;
  padding: 12rpx 24rpx;
  border-radius: 48rpx;
  box-shadow: 0 2rpx 8rpx rgba(240, 163, 194, 0.1);
  border: 2rpx solid #fef3c7;
}

.button-text {
  color: #e879a9;
  font-size: 22rpx;
  font-weight: 900;
}

/* 区块 */
.section {
  width: 100%;
  margin-bottom: 56rpx;
  animation: slideIn 0.5s ease-out;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
  padding: 0 8rpx;
}

.section-title {
  color: #b87a94;
  font-size: 30rpx;
  font-weight: 900;
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 30rpx;
}

.see-all {
  display: flex;
  align-items: center;
  gap: 4rpx;
}

.see-all-text {
  color: #f0a8c0;
  font-size: 24rpx;
  font-weight: 700;
}

.chevron {
  color: #f0a8c0;
  font-size: 28rpx;
  font-weight: 700;
}

/* 继续阅读卡片 */
.continue-reading-card {
  background: linear-gradient(135deg, #fff5f7 0%, #fdf2f8 100%);
  border: 4rpx solid rgba(252, 231, 243, 0.95);
  border-radius: 48rpx;
  box-shadow: 0 4rpx 16rpx rgba(232, 121, 169, 0.12);
  padding: 28rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
  transition: all 0.2s;
  box-shadow: 
  0 8rpx 16rpx rgba(0, 0, 0, 0.08), 
  inset 0 -4rpx 8rpx rgba(0, 0, 0, 0.05),
  inset 0 4rpx 8rpx rgba(255, 255, 255, 0.6);
}

.continue-reading-card:active {
  background: #fce7f3;
  transform: scale(0.98);
}

.book-cover {
  width: 112rpx;
  height: 112rpx;
  border-radius: 32rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  border: 4rpx solid white;
  flex-shrink: 0;
}

.book-details {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.book-title {
  color: #9d6b7f;
  font-size: 30rpx;
  font-weight: 900;
  margin-bottom: 8rpx;
}

.book-progress {
  color: #b87a94;
  font-size: 24rpx;
  font-weight: 600;
  margin-bottom: 12rpx;
}

.progress-bar {
  width: 100%;
  height: 12rpx;
  background: rgba(252, 231, 243, 0.9);
  border-radius: 24rpx;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #fbcfe8, #f0a3c2);
  border-radius: 24rpx;
  transition: width 0.3s ease;
}

.play-icon {
  font-size: 64rpx;
  flex-shrink: 0;
  box-shadow: 
  0 8rpx 16rpx rgba(0, 0, 0, 0.08),  
  inset 0 -4rpx 8rpx rgba(0, 0, 0, 0.05), 
  inset 0 4rpx 8rpx rgba(255, 255, 255, 0.6); 
}
/* 菜单项 */
.menu-items {
  display: flex;
  flex-direction: column;
  gap: 30rpx;
}

.menu-item {
  display: flex;
  align-items: center;
  gap: 38rpx;
  padding: 32rpx;
  border-radius: 48rpx;
  border: 4rpx solid white;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  transition: all 0.2s;
  position: relative;
  margin-bottom: 20rpx;
  overflow: visible;
  box-shadow: 
  0 8rpx 16rpx rgba(0, 0, 0, 0.08),  
  inset 0 -4rpx 8rpx rgba(0, 0, 0, 0.05), 
  inset 0 4rpx 8rpx rgba(255, 255, 255, 0.6); 
}

.menu-item:active {
  transform: translateY(2rpx); /* 按下时轻微下沉 */
  box-shadow: 
    0 4rpx 8rpx rgba(0, 0, 0, 0.08), 
    inset 0 -2rpx 4rpx rgba(0, 0, 0, 0.05),
    inset 0 2rpx 4rpx rgba(255, 255, 255, 0.6);
}
.menu-books {
  background: linear-gradient(135deg, #fff5f7 0%, #fef3c7 100%);
}

.menu-books:active {
  background: #fce7f3;
}

.menu-voice {
  background: linear-gradient(135deg, #fce7f3 0%, #fef3c7 100%);
}

.menu-voice:active {
  background: #fef3c7;
}

.menu-play {
  background: linear-gradient(135deg, #fce7f3 0%, #fbcfe8 100%);
}

.menu-play:active {
  background: #fbcfe8;
}

.menu-icon-box {
  background: rgba(255, 255, 255, 0.7);
  padding: 24rpx;
  border-radius: 32rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  border: 2rpx solid white;
  flex-shrink: 0;
}

.menu-icon {
  font-size: 56rpx;
}

.menu-text {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.menu-title {
  font-family: var(--app-font-family) !important;
  font-size: 40rpx;
  font-weight: 900;
  letter-spacing: 2rpx;
}

.menu-books .menu-title {
  font-family: var(--app-font-family) !important;
  color: #9d6b7f;
}

.menu-voice .menu-title {
  font-family: var(--app-font-family) !important;
  color: #b87a94;
}

.menu-play .menu-title {
  font-family: var(--app-font-family) !important;
  color: #c97a9a;
}

.menu-subtitle {
  background: #fef3c7;
  padding: 4rpx 16rpx;
  border-radius: 24rpx;
  margin-top: 8rpx;
  display: inline-flex;
  align-self: flex-start;
  border: 2rpx solid #fde047;
}

.subtitle-text {
  color: #713f12;
  font-size: 20rpx;
  font-weight: 900;
}


.menu-emoji-wrapper {

  position: absolute;
  right: 32rpx;
  top: 50%;
  transform: translateY(-50%);
  margin-right: 20rpx;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 贴纸图标：可放大超出框 */
.menu-icon-image-sticker {
  width: 200rpx;
  height: 200rpx;
  object-fit: contain;
  position: absolute;
  filter: drop-shadow(0 6rpx 12rpx rgba(0, 0, 0, 0.15));
}
.menu-icon-image-sticker1 {
  width: 150rpx;
  height: 150rpx;
  object-fit: contain;
  position: absolute;
  filter: drop-shadow(0 6rpx 12rpx rgba(0, 0, 0, 0.15));
}
.menu-icon-image-sticker2 {
  width: 180rpx;
  height: 180rpx;
  object-fit: contain;
  position: absolute;
  margin-right: 20rpx;
  filter: drop-shadow(0 6rpx 12rpx rgba(0, 0, 0, 0.15));
}

/* 其他菜单的 emoji 保持原样 */
.menu-emoji-text {
  font-size: 48rpx;
}

/* 任务滚动 */
.tasks-scroll {
  white-space: nowrap;
  padding-bottom: 8rpx;
}

.task-card {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  width: 288rpx;
  padding: 28rpx;
  border-radius: 48rpx;
  border: 4rpx solid white;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  margin-right: 24rpx;
  transition: all 0.2s;
}

.task-card:active {
  transform: scale(0.97);
}

.task-checkin {
  background: #ffedd5;
}

.task-checkin:active {
  background: #fed7aa;
}

.task-listen {
  background: linear-gradient(135deg, #fdf2f8 0%, #fef3c7 100%);
}

.task-listen:active {
  background: #fce7f3;
}

.task-color {
  background: #fce7f3;
}

.task-color:active {
  background: #fef3c7;
}

.task-icon-box {
  background: rgba(255, 255, 255, 0.5);
  padding: 20rpx;
  border-radius: 50%;
}

.task-icon {
  font-size: 48rpx;
}

.task-title {
  font-size: 26rpx;
  font-weight: 900;
}

.task-checkin .task-title {
  color: #7c2d12;
}

.task-listen .task-title {
  color: #b87a94;
}

.task-color .task-title {
  color: #b87a94;
}

.task-reward {
  background: white;
  padding: 8rpx 16rpx;
  border-radius: 24rpx;
  font-size: 22rpx;
  font-weight: 900;
  border: 2rpx solid rgba(0, 0, 0, 0.05);
}

.task-checkin .task-reward {
  color: #ea580c;
}

.task-listen .task-reward {
  color: #e879a9;
}

.task-color .task-reward {
  color: #e879a9;
}

/* 宠物蛋入口 */
.pet-entry-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 24rpx 0 8rpx;
  padding: 22rpx 24rpx;
  background: #ffffff;
  border-radius: 24rpx;
  border: 2rpx solid rgba(253, 230, 240, 0.55);
  box-shadow: 0 8rpx 24rpx rgba(232, 121, 169, 0.1);
}

.pet-entry-left {
  display: flex;
  align-items: center;
  gap: 18rpx;
}

.pet-entry-icon {
  font-size: 48rpx;
}

.pet-entry-texts {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.pet-entry-title {
  font-size: 28rpx;
  font-weight: 900;
  color: #9d4e6e;
}

.pet-entry-desc {
  font-size: 22rpx;
  color: #6b7280;
}

.pet-entry-right {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.pet-entry-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: #ef4444;
  box-shadow: 0 0 0 4rpx rgba(239, 68, 68, 0.25);
}

.pet-entry-arrow {
  font-size: 36rpx;
  color: #fbcfe8;
  font-weight: 700;
}

.avatar-option {
  position: relative;
}

.avatar-option-locked .option-image-dim {
  opacity: 0.55;
}

.avatar-frost {
  position: absolute;
  left: 8rpx;
  top: 8rpx;
  right: 8rpx;
  bottom: 52rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.5);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2rpx solid rgba(255, 255, 255, 0.7);
}

.avatar-frost-icon {
  font-size: 32rpx;
}

/* 3D按钮容器（保持原卡片尺寸） */
.metric-button-3d {
  width: 150rpx;
  height: 150rpx;
  border-radius: 50%;
  background: linear-gradient(145deg, #ffffff, #e6e9ec);
  box-shadow: 
    8rpx 8rpx 16rpx rgba(0, 0, 0, 0.1),
    -8rpx -8rpx 16rpx rgba(255, 255, 255, 0.9),
    inset 2rpx 2rpx 4rpx rgba(255, 255, 255, 0.8),
    inset -2rpx -2rpx 4rpx rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
}

.metric-button-3d {
  width: 150rpx;
  height: 150rpx;
  border-radius: 50%;
  /* 中间圆形：紫粉渐变 + 超强立体浮雕 */
  background: radial-gradient(circle at 30% 30%, #fdeaf4 0%, #f8cfe6 42%, #efadd1 100%);
  box-shadow: 
    0 12rpx 22rpx rgba(232, 121, 169, 0.3),
    0 4rpx 8rpx rgba(232, 121, 169, 0.22),
    inset 0 -6rpx 8rpx rgba(205, 94, 155, 0.22),
    inset 0 6rpx 10rpx rgba(255, 255, 255, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.25s ease;
  position: relative;
}

/* 按下立体效果 */
.metric-button-3d:active {
  transform: scale(0.95);
  box-shadow: 
    0 6rpx 10rpx rgba(232, 121, 169, 0.24),
    inset 0 -4rpx 6rpx rgba(205, 94, 155, 0.2),
    inset 0 4rpx 8rpx rgba(255, 255, 255, 0.7);
}

/* 按钮文字 */
.metric-button-text {
  font-size: 32rpx;
  font-weight: 900;
  color: #c05394;
  letter-spacing: 1rpx;
  position: relative;
  z-index: 2;
}


.metric-card {
  flex: 1;
  background: linear-gradient(145deg, #fff8fb 0%, #fdeef5 100%);
  border-radius: 36rpx;
  border: 3rpx solid rgba(250, 220, 234, 0.95);
  box-shadow: 0 10rpx 24rpx rgba(232, 121, 169, 0.12);
  padding: 22rpx 18rpx 18rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.guide-pulse {
  position: relative;
  z-index: 1001;
  isolation: isolate;
  animation: guidePulseRing 1.15s ease-in-out infinite;
}

@keyframes guidePulseRing {
  0%,
  100% {
    box-shadow: 0 0 0 3rpx rgba(240, 163, 194, 0.95), 0 6rpx 18rpx rgba(232, 121, 169, 0.16);
  }
  50% {
    box-shadow: 0 0 0 5rpx rgba(240, 163, 194, 0.62), 0 8rpx 22rpx rgba(232, 121, 169, 0.2);
  }
}

.guide-dim {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 1000;
  background: rgba(20, 20, 30, 0.35);
}

.guide-card-wrap {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1002;
  padding: 28rpx;
  box-sizing: border-box;
}

.guide-card {
  width: 100%;
  border-radius: 28rpx;
  background: #fff;
  padding: 24rpx;
  box-sizing: border-box;
  box-shadow: 0 -10rpx 36rpx rgba(0, 0, 0, 0.1);
}

.guide-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #f0a3c2;
  margin-bottom: 10rpx;
}

.guide-title {
  display: block;
  font-size: 30rpx;
  font-weight: 900;
  color: #a84f74;
}

.guide-desc {
  display: block;
  margin-top: 8rpx;
  font-size: 24rpx;
  color: #6b7280;
}

.guide-actions {
  margin-top: 16rpx;
  display: flex;
  justify-content: flex-end;
  gap: 12rpx;
}

.guide-btn {
  padding: 10rpx 20rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  font-weight: 800;
}

.guide-btn.ghost {
  background: #f3f4f6;
  color: #6b7280;
}

.guide-btn.primary {
  background: #f0a3c2;
  color: #fff;
}
