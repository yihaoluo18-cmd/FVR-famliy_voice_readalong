字体放置说明

请将你的字体文件放在本目录，文件名固定为：

- `cute-candy.ttf`（当前用于小程序字体加载的固定文件名）

推荐做法：

1. 将原文件 `字魂萌趣软糖体(商用需授权).ttf` 复制到本目录
2. 重命名为 `cute-candy.ttf`

后端启动后可通过以下地址访问：

- `http://127.0.0.1:9880/static/fonts/cute-candy.ttf`

前端已优先加载该地址。
